parameters:
  grafana:
    client:
      dashboard:
        grafana_prometheus:
          datasource: prometheus
          format: json
          template: grafana/files/dashboards/grafana/grafana_prometheus.json
