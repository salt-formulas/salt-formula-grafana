parameters:
  grafana:
    client:
      dashboard:
        calico_prometheus:
          datasource: prometheus
          format: json
          template: grafana/files/dashboards/kubernetes/calico_prometheus.json
