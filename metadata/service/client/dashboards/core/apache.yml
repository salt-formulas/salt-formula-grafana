parameters:
  grafana:
    client:
      dashboard:
        apache_prometheus:
          datasource: prometheus
          format: json
          template: grafana/files/dashboards/apache/apache_prometheus.json