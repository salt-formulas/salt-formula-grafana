parameters:
  grafana:
    client:
      dashboard:
        mysql_prometheus:
          datasource: prometheus
          format: json
          template: grafana/files/dashboards/galera/mysql_prometheus.json
